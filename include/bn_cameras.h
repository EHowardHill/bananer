#ifndef BN_CAMERAS_H
#define BN_CAMERAS_H

#include "bn_camera_ptr.h"

namespace bn::cameras
{
    // Stubs for global camera management if referenced by main.cpp
    // (If compilation fails later looking for specific functions here,
    // we will implement them then).
}

#endif